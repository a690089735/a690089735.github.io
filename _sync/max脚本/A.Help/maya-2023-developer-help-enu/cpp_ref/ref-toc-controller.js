
var cpp_ref_adsk_ref_toc = (function() {

var NAVTREE =
[
  [ "C++ API Reference", "index.html", [
    [ "Maya API Reference", "index.html", null ],
    [ "Deprecated List", "deprecated.html", null ],
    [ "Changes in 2023.0", "changelog_2023_0.html", null ],
    [ "Changes in 2022.0", "changelog_2022_0.html", null ],
    [ "Changes in 2020.0", "changelog_2020_0.html", null ],
    [ "Changes in 2019.0", "changelog_2019_0.html", null ],
    [ "Changes in 2018.0", "changelog_2018_0.html", null ],
    [ "Changes in 2017.0", "changelog_2017_0.html", null ],   
    [ "Changes in 2016.0", "changelog_2016_0.html", null ],
    [ "Changes in 2015.0", "changelog_2015_0.html", null ],
    [ "Modules", "modules.html", "modules" ],
    [ "Namespaces", null, [
      [ "Namespace List", "namespaces.html", "namespaces" ],
      [ "Namespace Members", "namespacemembers.html", [
        [ "All", "namespacemembers.html", null ],
        [ "Functions", "namespacemembers_func.html", null ]
      ] ]
    ] ],
    [ "Classes", "annotated.html", [
      [ "Class List", "annotated.html", "annotated_dup" ],
      [ "Class Index", "classes.html", null ],
      [ "Class Hierarchy", "hierarchy.html", "hierarchy" ],
      [ "Class Members", "functions.html", [
        [ "All", "functions.html", "functions_dup" ],
        [ "Functions", "functions_func.html", "functions_func" ],
        [ "Variables", "functions_vars.html", "functions_vars" ],
        [ "Typedefs", "functions_type.html", null ],
        [ "Enumerations", "functions_enum.html", "functions_enum" ],
        [ "Enumerator", "functions_eval.html", "functions_eval" ],
        [ "Related Functions", "functions_rela.html", null ]
      ] ]
    ] ],
    [ "Examples", "examples.html", "examples" ]
  ] ]
];

var NAVTREEINDEX =
[
".html",
"class_m3d_view.html#a169efed3046f92a7f5159d4e67207c31a8b8a643b1a54288a3339b99c95f565ab",
"class_m_anim_curve_change.html#ac2632f60022bbad00e81d0db4f90fecc",
"class_m_attribute_index.html#a83b3db12120d770aeeec91a1e95cd6af",
"class_m_callback_id_array.html#a8c66dfa06f3736b2a214c5aed58bd216",
"class_m_common_system_utils.html#a774cd5d8fbebe8e7ed82a5aa587d1f04",
"class_m_dag_modifier.html#ae169eb7f1ddc00f53ba69ac2d1815950",
"class_m_distance.html#aac58671a639b69d01db22fc7655e17cc",
"class_m_euler_rotation.html#af3d74c777e523f2725cdf87c15a4b5bca0ede29653f0c0e677e80ddd6f6bd84d4",
"class_m_file_object.html#a4a5fc51336041210363dc02b26533452",
"class_m_float_vector.html#ac02d7e1c81087ef27fc192bb02ced62e",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a10af787938972a6f50efabb015e05978",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a2e566bd58825ea4de7d6afeaee7c1292",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a4d40ba13f549813391360477b926eaec",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a6916cc058bdd2209fbac1445a964047c",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a81d7b49e6d0ca8ae7e6676ac749ddd5a",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7a9ac8230509b133ee0b02e24823d469c3",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7abb399bce9f909b57c1d01b2b04de3eb4",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7adafa274206df602835f0f22ae887e126",
"class_m_fn.html#a1d1cfd8ffb84e947f82999c682b666a7af50edc5b2b0c8a4ee85f8cbd233d4e73",
"class_m_fn_anim_curve.html#a6b26eee66e19b558a138d0c009653e51a57c65dfa75af8df3dc8710ee9a710e98",
"class_m_fn_assembly.html#a000aca87b65d143955e5dca2cea0d671",
"class_m_fn_blend_shape_deformer.html#a25e1c85ae8d37804a543a0ad4da41f29",
"class_m_fn_camera.html#a8607e1142843f35474557b5daa9489de",
"class_m_fn_circle_sweep_manip.html#a0f82e291ea91228c4c394e04dd5fa48e",
"class_m_fn_container_node.html#af813a53d6034ef00a8969f266a572fce",
"class_m_fn_data.html#afc95d5d42f7cdded0243cb47d6bbc903",
"class_m_fn_direction_manip.html#af1adb4abaf5243c6c0749bcca7b5c418",
"class_m_fn_enum_attribute.html#acded485b8f3194d8a7a6b53100d48559",
"class_m_fn_fluid.html#a83d666c6f40b4df3a5d0e652cc02bb06",
"class_m_fn_geometry_filter.html#a8d0c12dca7045e5aa238a2144e32ae04",
"class_m_fn_ik_joint.html#afb37ac5d1c5ce81e93ea7832fe977ba9",
"class_m_fn_keyframe_delta_scale.html#a50ed55f78ebcd59a272b1a44a2d20aa5",
"class_m_fn_light.html#a36ccaa30668f9bef9154f39d61cf27b5",
"class_m_fn_mesh.html#a21f54d9e1ca3cbed6492f576f9f61215",
"class_m_fn_mesh.html#a742ce017199905f06ad07c178375a2d8",
"class_m_fn_mesh.html#adfaf9bba50f6afd2dad942ccf15daf30",
"class_m_fn_n_object_data.html#aefd19c813ac34e7f10953f676da69cdb",
"class_m_fn_numeric_data.html#a1d1cfd8ffb84e947f82999c682b666a7a298f4738d8df72232a1e19267767b1aa",
"class_m_fn_nurbs_surface.html",
"class_m_fn_particle_system.html#a29cdc61828182d6a418f15b9798c4567",
"class_m_fn_plugin.html#a35eed70ac4287b860f273f4ec852f577",
"class_m_fn_point_on_surface_manip.html#a25107bb7a39038b1605c4837e57877fd",
"class_m_fn_scale_manip.html#a1f4835e564152d6777cf813dc1007e81",
"class_m_fn_spot_light.html#a96f02015202c58638d2192a66ec305ca",
"class_m_fn_string_array_data.html#a7039603a3b9f8eab32efad3c5a31a176",
"class_m_fn_subd_names.html#a6c729102e5aaf7a5eb09482d73901b6b",
"class_m_fn_transform.html#afc1cb5e455ffe968fe90196edf0082ee",
"class_m_fn_unit_attribute.html#a563766a920e62cba567fdc71af3be8e4",
"class_m_fn_vortex_field.html#adc3250674414e8a0b2f4584959c6ec4c",
"class_m_geometry_primitive.html#a9daf68d761d48b661dab360616d4ad84",
"class_m_h_w_render_1_1_m_blend_state.html#ad3316ff174eff6a45c45f0e90d84eabea11a8c07455b6c1d80c87e414be979962",
"class_m_h_w_render_1_1_m_frame_context.html#a412d9516b93bcc54229ad6b895baa1aea9feb655e881b3bb2a044f1568704602e",
"class_m_h_w_render_1_1_m_intersection.html#a2fa1d445309fffa9993954542d8610c0",
"class_m_h_w_render_1_1_m_px_index_buffer_mutator.html#abfdf7c328f05979c1abe0b1ac5f9a007",
"class_m_h_w_render_1_1_m_px_viewport_compute_item.html#a8bb1ef53467e4f61410d12822d922498ae4da30b045a45e55bc6158eef1dd472d",
"class_m_h_w_render_1_1_m_render_parameters.html#a81c36a583812e1f7dbd27166c58a4661",
"class_m_h_w_render_1_1_m_scene_render.html#afbcfbcd49fe6e70be75e490e66514acaa02cb81a85144133d15b64cd88aa4a0bd",
"class_m_h_w_render_1_1_m_texture.html#ac556578d6219804b38b9387f8c344ef7",
"class_m_hardware_renderer.html#a14dafbca9ec0aa5132598b888511bfcba1ef6c2d725fb4bec3e7e840d28adbc00",
"class_m_it_curve_c_v.html#a0a76826cbe7a57cd7734727c46084145",
"class_m_it_mesh_edge.html#af549faf11505089d3121c367a386aa69",
"class_m_library.html#a0b0578d8dd1539bcdead076debc5e8ba",
"class_m_mesh_smooth_options.html#a661593ebca7dfd6546c8b2669ad95a71a5cf9af5a30b6b4396f929c93928b1cc9",
"class_m_object_handle.html#a2fd2d2ffbeac68aa3d10b287d0f74379",
"class_m_plug.html#aa74cccce6c73be4bcac408cb214bcda1",
"class_m_profiling_scope.html#ad980017ff0631e7ca146535092d2b26a",
"class_m_px3d_model_view.html#ae1825c4a12851ce4530188fae98832d0",
"class_m_px_cache_format.html#a248681daa33e11b68c934862cfe49417a7df238f9e1ead00d81f7b659517e9f5a",
"class_m_px_constraint.html#a45de359e76132461563e1e39ba21190b",
"class_m_px_control_command.html#a774cd5d8fbebe8e7ed82a5aa587d1f04",
"class_m_px_emitter_node.html#afb082a32b59d04695e81e7f9764239fc",
"class_m_px_g_p_u_standard_deformer.html#a868e9a8fd4c395687c82e9e90dc3a508",
"class_m_px_hw_shader_node.html#a495a60eaa1df3815d5565e79e08c90ac",
"class_m_px_image_plane.html#a202b9f8ce355b81620f4117cbe6bdeb7",
"class_m_px_manip_container.html#a05c4f986be15ca9b62dd2c5cb317c57ca0de23205dec501371d6383f95d943254",
"class_m_px_material_information.html#ac5eb5f0bbb46234964828071e09e897d",
"class_m_px_multi_poly_tweak_u_v_command.html#ac2e266483f34c2dd62ec81490366a0d5",
"class_m_px_particle_attribute_mapper_node.html#a7312c50410d0282e9a04637b94bee2ee",
"class_m_px_skin_cluster.html#a57ea39dc62f04da1e5600cf6f0753f91",
"class_m_px_surface_shape_u_i.html#a3dbfd21ce512aeb2c824f06e346a47f0",
"class_m_px_transform.html#a2a04b796c2138e8432693482132a730e",
"class_m_px_transform.html#a9e51580873f4e1987818f5b099d9cfef",
"class_m_px_transformation_matrix.html#a1830b945686cc9c2b1558de2ba899757",
"class_m_quaternion.html#a369e979016c6727e1dfe57c26f518907",
"class_m_render_view.html#a83fe29675c567275fe349381ee1f6f2d",
"class_m_selection_list.html#a5f91db371743baee3ae70fdc9fbcd2c0",
"class_m_string.html#a4b88bbf0589238870929ba88186d10f2",
"class_m_texture_editor_draw_info.html#ae1a71e91dcfaac359b887ff577844739",
"class_m_transformation_matrix.html#a1b988043b114c9e975ff8e332559ba6d",
"class_m_uniform_parameter.html#ad8ed01ff3ff33333d8e19db4d2818bb6a7d3972f1f1ee99452d3c715b157b5cad",
"class_m_vector.html#ac1ece8a677e478488cc4875591925ce0",
"class_mn_solver.html#aeb922e6d14c361b3bca6d48dfe0305b5",
"classadsk_1_1_data_1_1_index_string.html#ab5b56a7e2515dc71bf1cdc2ba3f0a5b4",
"dx11_shader_2dx11_cone_angle_to_hotspot_converter_8h-example.html",
"gpu_cache_2gpu_cache_spatial_subdivision_8h-example.html",
"scan_dag_cmd_2scan_dag_cmd_8cpp-example.html",
"view_render_override_targets_2view_render_override_targets_main_8cpp-example.html"
]; 
 var pub = {};

var storage_namespace = window.location.pathname.substring(0,window.location.pathname.lastIndexOf('/')); 
var sidenav,navtree,content,header;

function localStorageSupported()
{
  try {
    return 'localStorage' in window && window['localStorage'] !== null && window.localStorage.getItem;
  }
  catch(e) {
    return false;
  }
}

function anyStorageSupported()
{
  // test cookies
    var cookieEnabled = false;
	document.cookie="testcookie";
	cookieEnabled = (document.cookie.indexOf("testcookie") != -1) ? true : false;
	if (cookieEnabled)  { return true; }
  // test localStorage
  return localStorageSupported();
}

pub.readFromStorage = function(cookie) 
{
  if (localStorageSupported())
  {
	return window.localStorage.getItem(storage_namespace + "_" + cookie);
  }
  var myCookie = storage_namespace+"_"+cookie+"=";
  if (document.cookie) 
  {
    var index = document.cookie.indexOf(myCookie);
    if (index != -1) 
    {
      var valStart = index + myCookie.length;
      var valEnd = document.cookie.indexOf(";", valStart);
      if (valEnd == -1) 
      {
        valEnd = document.cookie.length;
      }
      var val = document.cookie.substring(valStart, valEnd);
      return val;
    }
  }
  return '';
}

pub.writeToStorage = function(cookie, val, expiration) 
{
  if (localStorageSupported())
  {
	window.localStorage.setItem(storage_namespace + "_" + cookie, val);
	return;
  }
  if (val==undefined) return;
  if (expiration == null) 
  {
    var date = new Date();
    date.setTime(date.getTime()+(10*365*24*60*60*1000)); // default expiration is one week
    expiration = date.toGMTString();
  }
  document.cookie = storage_namespace + "_" + cookie + "=" + val + "; expires=" + expiration+"; path=/";
}
 
function resizeWidth() 
{
  var windowWidth = dQuery(window).width() + "px";
  var sidenavWidth = dQuery(sidenav).outerWidth();
  content.css({marginLeft:parseInt(sidenavWidth)+"px"}); 
  pub.writeToStorage('width',sidenavWidth, '');
}

function restoreWidth(navWidth)
{
  var windowWidth = dQuery(window).width() + "px";
  content.css({marginLeft:parseInt(navWidth)+6+"px"});
  sidenav.css({width:navWidth + "px"});
}

function resizeHeight() 
{
  var headerHeight = (typeof(header) == 'undefined') ? 0 : header.outerHeight();
  var footerHeight = (typeof(footer) == 'undefined') ? 0 : footer.outerHeight();
  var windowHeight = dQuery(window).height() - headerHeight - footerHeight;
  if (!dQuery("div#main.view-active").length) { 
    // we are not in the 2.1 chrome: therefore AKN or CHM
    dQuery("#side-nav").css("position", "fixed"); 
    dQuery("#doc-content").css("min-height", windowHeight + "px"); 
    }
  navtree.css({height:windowHeight + "px"});
  sidenav.css({height:windowHeight + "px",top: 0+"px"});
}

pub.initResizable = function()
{
  header  = dQuery("#banner");
  sidenav = dQuery("#side-nav");
  content = dQuery("#doc-content");
  navtree = dQuery("#nav-tree");
  footer  = dQuery("#breadcrumbs");
  dQuery(".side-nav-resizable").resizable({resize: function(e, ui) { resizeWidth(); } });
  dQuery(window).resize(function() { resizeHeight(); });
  var width = pub.readFromStorage('width');
  if (width) { restoreWidth(width); } else { resizeWidth(); }
  resizeHeight();
  var url = location.href;
  var i=url.indexOf("#");
  if (i>=0) window.location.hash=url.substr(i);
  var _preventDefault = function(evt) { evt.preventDefault(); };   
  dQuery("#splitbar").bind("dragstart", _preventDefault).bind("selectstart", _preventDefault);
  dQuery(document).bind('touchmove',function(e){
    var device = navigator.userAgent.toLowerCase();
    var ios = device.match(/(iphone|ipod|ipad)/);
    if (ios) {
      try {
        var target = e.target;
        while (target) {
          if (dQuery(target).css('-webkit-overflow-scrolling')=='touch') return;
          target = target.parentNode;
        }
        e.preventDefault();
      } catch(err) {
        e.preventDefault();
      }
    }
  });
}



var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var navTreeSubIndices = [];

function getData(varName)
{
  var i = varName.lastIndexOf('/');
  var n = i>=0 ? varName.substring(i+1) : varName;
  return eval(n.replace(/\-/g,'_'));
}

// this works in BOTH normal case and 2.1 to return the filename of the current HTML file.
function getFilename(uri)
{
    var lastSlash = uri.lastIndexOf('/');
	var lastHashMark = uri.lastIndexOf('#');
	if (lastHashMark < lastSlash)
	{ link = uri.substring(lastSlash + 1); } 
	else
	{ link = uri.substring(lastSlash + 1, lastHashMark); }
	return link;
}

// works in both 2.1 and normal case, IF used on href and not on pathname
// Do not use on attr('pathname') in to get the current filename. Use only on attr('href')
function stripPath(uri)
{
  return uri.substring(uri.lastIndexOf('/')+1);
}

// works in both 2.1 and normal case, IF used on href
// Always gives the real within-page anchor, if any.
function getHash(uri)
{
	lastHash = uri.lastIndexOf('#');
	if (lastHash == -1) // we are not in 2.1, and there is no hash.
		return '';
	UriHash = uri.substring(lastHash);
	if (UriHash.indexOf('#!/url=') == -1) // whether we're in 2.1 or not, there is a hash, so return it.
		return UriHash;
	return ''; // we are in 2.1, and there is no hash.
}

// This is supposed to support using doxygen with multiple subdirs or short naming scheme.
// In most cases I think it should return the same as above.
function stripPath2(uri)
{
  var i = uri.lastIndexOf('/');
  var s = uri.substring(i+1);
  var m = uri.substring(0,i+1).match(/\/d\w\/d\w\w\/$/); // if the path matches /d./d../$ WHY????
  return m ? uri.substring(i-6) : s;
}


function removeExpandedNodeFromCache(node)
{
	// TODO: also remove all children?
	var expandedNodes = [];
	expandedNodes = cachedExpandedNodes();
	indexOfNode = dQuery.inArray(node.id, expandedNodes);
	if (indexOfNode != -1)
	{
		expandedNodes.splice(indexOfNode, 1);
		pub.writeToStorage('expandedNodes',JSON.stringify(expandedNodes));
	}
}

function expandedInCache(inId)
{
	var expandedNodes = [];
	expandedNodes = cachedExpandedNodes();
	if (dQuery.inArray(inId, expandedNodes) != -1)
		return true;
	return false;
}

function cachedExpandedNodes()
{
	var expandedNodes = [];
	cachedData = pub.readFromStorage('expandedNodes');
	if (cachedData)
	{
		expandedNodes = JSON.parse(cachedData);
		return expandedNodes;
	}
	return [];
}

function storeExpandedNode(node)
{
	var expandedNodes = [];
	expandedNodes = cachedExpandedNodes();
	if (dQuery.inArray(node.id, expandedNodes) == -1)
	{
		expandedNodes.push(node.id);
		pub.writeToStorage('expandedNodes', JSON.stringify(expandedNodes));
	}
}

function setUseCachedBreadcrumbs(doitornot)
{
	if (doitornot)
		pub.writeToStorage('useBreadcrumbs','true');
	else
		pub.writeToStorage('useBreadcrumbs','');
}
function getUseCachedBreadcrumbs()
{
	return pub.readFromStorage('useBreadcrumbs');
}


function storeBreadcrumbs(bc)
{
	pub.writeToStorage('breadcrumbs',JSON.stringify(bc));
}
function deleteBreadcrumbs()
{
	pub.writeToStorage('breadcrumbs','[]');
}

function cachedBreadcrumbs()
{
	var crumbs = pub.readFromStorage('breadcrumbs')
	if (!crumbs) return [];
	return JSON.parse(crumbs);
}

function storeLink(link)
{
	if (!dQuery("#nav-sync").hasClass('sync')) {
		pub.writeToStorage('navpath',link);
	}
}

function deleteLink()
{
	pub.writeToStorage('navpath','');
}

function cachedLink()
{
	var linkInCache = pub.readFromStorage('navpath');
	if (!linkInCache) return '';
	return linkInCache;
}

function getScript(scriptName,func,show)
{
  var head = document.getElementsByTagName("head")[0]; 
  var script = document.createElement('script');
  script.id = scriptName;
  script.type = 'text/javascript';
  script.onload = func; 
  script.src = scriptName+'.js'; 
  if (dQuery.browser.msie && dQuery.browser.version<=8) { 
    // script.onload does not work with older versions of IE
    script.onreadystatechange = function() {
      if (script.readyState=='complete' || script.readyState=='loaded') { 
        func(); if (show) showRoot(); 
      }
    }
  }
  head.appendChild(script); 
}

function createIndent(o,domNode,node,level)
{
  var level=-1;
  var n = node;
  while (n.parentNode) { level++; n=n.parentNode; }
  if (node.childrenData) {
    var imgNode = document.createElement("img");
    imgNode.style.paddingLeft=(16*level).toString()+'px';
    imgNode.width  = 16;
    imgNode.height = 22;
    imgNode.border = 0;
    node.plus_img = imgNode;
    node.expandToggle = document.createElement("a");
    node.expandToggle.href = "javascript:void(0)";
    node.expandToggle.onclick = function() {
      if (node.expanded) {
        dQuery(node.getChildrenUL()).slideUp("fast");
        node.plus_img.src = node.relpath+"arrowright.png";
        node.expanded = false;
		//removeExpandedNodeFromCache(node);
      } else {
        expandNode(o, node, false, false);
      }
    }
    node.expandToggle.appendChild(imgNode);
    domNode.appendChild(node.expandToggle);
    imgNode.src = node.relpath+"arrowright.png";
  } else {
    var span = document.createElement("span");
    span.style.display = 'inline-block';
    span.style.width   = 16*(level+1)+'px';
    span.style.height  = '22px';
    span.innerHTML = '&#160;';
    domNode.appendChild(span);
  } 
}

var animationInProgress = false;

function gotoAnchor(anchor,aname,updateLocation)
{
    if (dQuery("div#main.view-active").length) { return; } // this lets the 2.1 chrome do the scrolling.
  var pos, docContent = dQuery('#doc-content');
  if (anchor.parent().attr('class')=='memItemLeft' ||
      anchor.parent().attr('class')=='fieldtype' ||
      anchor.parent().is(':header')) 
  {
    pos = anchor.parent().position().top;
  } else if (anchor.position()) {
    pos = anchor.position().top;
  }
  if (pos) {
    var dist = Math.abs(Math.min(
               pos-docContent.offset().top,
               docContent[0].scrollHeight-
               docContent.height()-docContent.scrollTop()));
    animationInProgress=true;
    docContent.animate({
      scrollTop: pos + docContent.scrollTop() - docContent.offset().top
    },Math.max(50,Math.min(500,dist)),function(){
      if (updateLocation) window.location.href=aname;
      animationInProgress=false;
    });
  }
}

function newNode(o, po, text, link, childrenData, indexInParent)
{
  var node = {};
  node.children = [];
  node.childrenData = childrenData;
  node.depth = po.depth + 1;
  node.relpath = po.relpath;
  node.isLast = indexInParent==po.childrenData.length-1;
  node.breadcrumbs = (typeof(po.breadcrumbs) != "undefined") ? po.breadcrumbs.concat(indexInParent) : [indexInParent];

  node.li = document.createElement("li");
  po.getChildrenUL().appendChild(node.li);
  node.parentNode = po;
  node.id = po.id + "." + text;

  node.itemDiv = document.createElement("div");
  node.itemDiv.className = "item";

  node.labelSpan = document.createElement("span");
  node.labelSpan.className = "label";

  createIndent(o,node.itemDiv,node,0);
  node.itemDiv.appendChild(node.labelSpan);
  node.li.appendChild(node.itemDiv);

  var a = document.createElement("a");
  node.labelSpan.appendChild(a);
  node.label = document.createTextNode(text);
  node.expanded = false;
  a.appendChild(node.label);
  if (link) {
    var url;
    if (link.substring(0,1)=='^') {
      url = link.substring(1);
      link = url;
    } else {
      var prefix = "";
	if (dQuery("div#main.view-active").length) // we are in ADE 2.1
	{ prefix = "./index.html#!/url=./"; }
	url = prefix+node.relpath+link;

    }
    a.className = stripPath(link.replace('#',':'));
    if (link.indexOf('#')!=-1) {
      var aname = '#'+link.split('#')[1];
      var srcPage = getFilename(dQuery(location).attr('href'));
      var targetPage = stripPath(link.split('#')[0]);
      a.href = (srcPage!=targetPage || dQuery("div#main.view-active").length) ? url : "javascript:void(0)"; 
      a.onclick = function(){
		storeBreadcrumbs(node.breadcrumbs);
		setUseCachedBreadcrumbs(true);
        if (!dQuery(a).parent().parent().hasClass('selected'))
        {
          dQuery('.item').removeClass('selected');
          dQuery('.item').removeAttr('id');
          dQuery(a).parent().parent().addClass('selected');
          dQuery(a).parent().parent().attr('id','selected');
        }
        var anchor = dQuery(aname.substring(aname.lastIndexOf("#")))
        gotoAnchor(anchor,aname,true);
      };
    } else {
      a.href = url;
      a.onclick = function() { storeBreadcrumbs(node.breadcrumbs); setUseCachedBreadcrumbs(true);}
    }
  } else {
    if (childrenData != null) 
    {
      a.className = "nolink";
      a.href = "javascript:void(0)";
      a.onclick = node.expandToggle.onclick;
    }
  }

  node.childrenUL = null;
  node.getChildrenUL = function() {
    if (!node.childrenUL) {
      node.childrenUL = document.createElement("ul");
      node.childrenUL.className = "children_ul";
      node.childrenUL.style.display = "none";
      node.li.appendChild(node.childrenUL);
    }
    return node.childrenUL;
  };

  return node;
}

function getDesiredOffset()
{
	var selectedItem = dQuery('#nav-tree #selected')
	var selectedItemTop = selectedItem.offset().top;
	var selectedItemBottom = selectedItemTop + selectedItem.height();
	var divTop = dQuery('#nav-tree').offset().top;
	var divBottom = divTop + dQuery('#nav-tree').height();
	if (selectedItemTop < divTop || selectedItemBottom > divBottom) return (dQuery('#nav-tree').height() / -5);
	return -1;
}

function showRoot()
{
  var headerHeight = dQuery("#top").height();
  var footerHeight = dQuery("#nav-path").height();
  var windowHeight = dQuery(window).height() - headerHeight - footerHeight;
  (function (){ // retry until we can scroll to the selected item
    try {
      var navtree=dQuery('#nav-tree');
	  var desiredOffset = getDesiredOffset();
	  if ( desiredOffset != -1 )
	  {
		navtree.scrollTo('#selected',0,{offset:desiredOffset});
	  }
  } catch (err) {
      setTimeout(arguments.callee, 0);
    }
  })();
}

function expandNode(o, node, imm, showRoot)
{
  if (node.childrenData && !node.expanded) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        expandNode(o, node, imm, showRoot);
      }, showRoot);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      } if (imm || (dQuery.browser.msie && dQuery.browser.version>8)) { 
        // somehow slideDown jumps to the start of tree for IE9 :-(
        dQuery(node.getChildrenUL()).show();
      } else {
        dQuery(node.getChildrenUL()).slideDown("fast");
      }
      node.plus_img.src = node.relpath+"arrowdown.png";
      node.expanded = true;
		//storeExpandedNode(node);
     }
  }
}

function glowEffect(n,duration)
{
  n.addClass('glow').delay(duration).queue(function(next){
    dQuery(this).removeClass('glow');next();
  });
}

function highlightAnchor()
{
  var aname = dQuery(location).attr('hash');
  var anchor = dQuery(aname.substring(aname.lastIndexOf("#")));
  if (typeof(toggleDetails) != "undefined")
	toggleDetails(aname.substring(aname.lastIndexOf("#") + 1), true);
  if (anchor.parent().attr('class')=='memItemLeft' || anchor.parent().attr('class')=='memItemRight'){
    var rows = dQuery('.memberdecls tr[class$="'+
               getHash(dQuery(location).attr('href')).substring(1)+'"]');
    glowEffect(rows.children(),750); // member without details
  } else if (anchor.parent().attr('class')=='groupheader'){
    glowEffect(anchor.parent(),800); // member without details
  } else if (anchor.parents().slice(2).prop('tagName')=='TR') {
    glowEffect(anchor.parents('div.memitem'),1250); // enum value
  } else if (anchor.parent().attr('class')=='fieldtype'){
    glowEffect(anchor.parent().parent(),1250); // struct field
  } else if (anchor.parent().is(":header")) {
    glowEffect(anchor.parent(),1250); // section header
  } else {
    glowEffect(anchor.next(),1250); // normal member
  }
  gotoAnchor(anchor,aname,false);
}

function selectAndHighlight(hash,n)
{
  var a;
  if (hash) {
    var link=getFilename(dQuery(location).attr('href'))+':'+hash.substring(1);
    a=dQuery('.item a[class$="'+link+'"]');
  }
  if (a && a.length) {
     dQuery('.item').removeClass('selected');
     dQuery('.item').removeAttr('id');
  	a.filter(":visible").each( function() {
    $(this).parent().parent().addClass('selected');
    $(this).parent().parent().attr('id','selected');
     } );
  } else if (n) {
     dQuery('.item').removeClass('selected');
     dQuery('.item').removeAttr('id');
    dQuery(n.itemDiv).addClass('selected');
    dQuery(n.itemDiv).attr('id','selected');
    storeBreadcrumbs(n.breadcrumbs);
    }
  if (dQuery('#nav-tree-contents .item:first').hasClass('selected')) {
    dQuery('#nav-sync').css('top','30px');
  } else {
    dQuery('#nav-sync').css('top','5px');
  }
  highlightAnchor();
  showRoot();
}

function showNode(o, node, index, hash)
{
  if (node && node.childrenData) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        showNode(o,node,index,hash);
      },true);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      }
      dQuery(node.getChildrenUL()).css({'display':'block'});
      node.plus_img.src = node.relpath+"arrowdown.png";
      node.expanded = true;
	  var n = node.children[o.breadcrumbs[index]];
	  if (typeof(n) != "undefined")
	  {
	  if (index+1<o.breadcrumbs.length) {
	    expandNode(o, n, true, true);
		showNode(o,n,index+1,hash);
	  } else {
		if (typeof(n.childrenData)==='string') {
		  var varName = n.childrenData;
		  getScript(n.relpath+varName,function(){
			n.childrenData = getData(varName);
			node.expanded=false;
			showNode(o,node,index,hash); // retry with child node expanded
		  },true);
		} else {
		  var rootBase = stripPath(o.toroot.replace(/\..+$/, ''));
		  if (rootBase=="index" || rootBase=="pages" || rootBase=="search") {
			expandNode(o, n, true, true);
		  }
		  expandNode(o, n, true, true);
		  selectAndHighlight(hash,n);
		}
	  } }
    }
  } else {
    selectAndHighlight(hash);
  }
}

function removeToInsertLater(element) {
  var parentNode = element.parentNode;
  var nextSibling = element.nextSibling;
  parentNode.removeChild(element);
  return function() {
    if (nextSibling) {
      parentNode.insertBefore(element, nextSibling);
    } else {
      parentNode.appendChild(element);
    }
  };
}

function getNode(o, po)
{
  var insertFunction = removeToInsertLater(po.li);
  po.childrenVisited = true;
  for (var i = 0; i < po.childrenData.length; i++) {
    var nodeData = po.childrenData[i];
    po.children[i] = newNode(o, po, nodeData[0], nodeData[1], nodeData[2],
      i);
	//if (expandedInCache(po.children[i].id)) { expandNode(o, po.children[i], true, true); }
  }
  insertFunction();
}

function gotoNode(o,subIndex,root,hash,relpath)
{
  var nti = navTreeSubIndices[subIndex][root+hash];
  o.breadcrumbs = dQuery.extend(true, [], nti ? nti : navTreeSubIndices[subIndex][root]);
  if (!o.breadcrumbs && root!=NAVTREE[0][1]) { // fallback: show index
    navTo(o,NAVTREE[0][1],"",relpath);
    dQuery('.item').removeClass('selected');
    dQuery('.item').removeAttr('id');
  }
  if (o.breadcrumbs) {
    o.breadcrumbs.unshift(0); // add 0 for root node
    showNode(o, o.node, 0, hash);
  }
}

function navTo(o,root,hash,relpath)
{
  var bc = cachedBreadcrumbs();
  if (getUseCachedBreadcrumbs() && bc && bc.length > 0) {
    o.breadcrumbs = bc;
	showNode(o, o.node, 0, '');
	if (dQuery('#nav-sync').hasClass('sync')) { setUseCachedBreadcrumbs(false); }
 	return;
  }
  if (hash.match(/^#l\d+$/)) {
    var anchor=dQuery('a[name='+hash.substring(1)+']');
    glowEffect(anchor.parent(),1250); // line number
    hash=''; // strip line number anchors
  }
  var url=root+hash.substring(hash.lastIndexOf("#"));
  var i=-1;
  while (NAVTREEINDEX[i+1]<=url) i++;
  if (i==-1) { i=0; root=NAVTREE[0][1]; } // fallback: show index
  if (navTreeSubIndices[i]) {
    gotoNode(o,i,root,hash,relpath)
  } else {
    getScript(relpath+'navtreeindex'+i,function(){
      navTreeSubIndices[i] = eval('NAVTREEINDEX'+i);
      if (navTreeSubIndices[i]) {
        gotoNode(o,i,root,hash,relpath);
      }
    },true);
  }
}

function showSyncOff(n,relpath)
{
    n.html('<img src="'+relpath+'sync_off.png" title="'+SYNCOFFMSG+'"/>');
    pub.writeToStorage('sync-state-off', 'true');
}

function showSyncOn(n,relpath)
{
    n.html('<img src="'+relpath+'sync_on.png" title="'+SYNCONMSG+'"/>');
    pub.writeToStorage('sync-state-off', '');
}

function toggleSyncButton(relpath)
{
  var navSync = dQuery('#nav-sync');
  if (navSync.hasClass('sync')) {
    navSync.removeClass('sync');
    showSyncOff(navSync,relpath);
    setUseCachedBreadcrumbs(true);
  } else {
    navSync.addClass('sync');
    showSyncOn(navSync,relpath);
    setUseCachedBreadcrumbs(false);
  }
}

pub.initNavTree = function(toroot,relpath)
{
  var o = {};
  o.toroot = toroot;
  o.node = {};
  o.node.li = document.getElementById("nav-tree-contents");
  o.node.childrenData = NAVTREE;
  o.node.children = [];
  o.node.childrenUL = document.createElement("ul");
  o.node.getChildrenUL = function() { return o.node.childrenUL; };
  o.node.li.appendChild(o.node.childrenUL);
  o.node.depth = 0;
  o.node.relpath = relpath;
  o.node.expanded = false;
  o.node.isLast = true;
  o.node.plus_img = document.createElement("img");
  o.node.plus_img.src = relpath+"arrowright.png";
  o.node.plus_img.width = 16;
  o.node.plus_img.height = 22;
  o.node.id = "root";
  o.node.breadcrumbs = [];

  if (anyStorageSupported())
  {
    var navSync = dQuery('#nav-sync');
    if (!pub.readFromStorage('sync-state-off')) {
    	showSyncOn(navSync,relpath);
    } else {
      showSyncOff(navSync,relpath);
      navSync.removeClass('sync');
    } 
    navSync.click(function(){ toggleSyncButton(relpath); });
  }
  
  dQuery(document).off("toc_initialized");
  dQuery(document).on("toc_initialized", function(){
    navTo(o,toroot,getHash(dQuery(location).attr('href')),relpath);
    showRoot();
  });

  dQuery(window).unbind('hashchange');
  dQuery(window).bind('hashchange', function() {
     if (getHash(dQuery(location).attr('href'))){
		if (getUseCachedBreadcrumbs() || getHash(dQuery(location).attr('href')) == "")
        	navTo(o,getFilename(dQuery(location).attr('href')),getHash(dQuery(location).attr('href')),relpath); 
        else
			selectAndHighlight(getHash(dQuery(location).attr('href')), null);
     } 
  })
}

return pub;
}());