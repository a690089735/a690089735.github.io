-- fn do_hello =
-- (
--     print"hello!!"
-- )
-- do_hello()

-- print sysInfo.currentdir --只有当手动运行时才会自然跳转
-- print (getThisScriptFilename()) --更好的获取路径

-- print (getfilenamepath(getThisScriptFilename())) --获取上级目录,注意:结尾是有斜杠的.

getnodebyname dem --报错会有点问题.