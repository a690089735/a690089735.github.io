刚需(经常会用到)
红色的建议需要付出20%就能提升动画师80%体验

功能
可实时更新的变形(而不是仅仅一个静态数据)(这个虽然不是经常会用到,但是一旦用到,可以大幅提高绑定自由度和动画效果.)
优化的锁定权重工具(锁定权重不应仅避免修改,同时也要影响到顶点的其他权重组的归一化计算)
可随意修改的动画层.

工具
于权重工具操作模式统一的权重绘制工具(按住添加,Ctrl减少,shift平滑)
实时的,模型及骨骼洋葱皮.(可按距逐帧,可仅关键帧,可仅固定帧,但都可以实时更新.)
可编辑的可视化运动轨迹.

UX
时间轴刻度
更简单的约束界面(按变换,位置,旋转,缩放分组,目前的组织方式只适合脚本化绑定,大多数动画师只是想自己随手绑定一些简单的东西,要么开箱即用,要么就让分组明确一点.)

IO
可以导出的BendyBone

曲线
曲线缓存(随时切换新旧版曲线,既可以同时存在两个版本曲线,又可以实时对比修改)
按曲线反调节手柄(类ps钢笔工具)
晶格调节曲线(类maya)


非刚需

约束
便于使用的物理骨骼约束

工具
基于dembones的权重解算或优化的工具

